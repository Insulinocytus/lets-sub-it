# whisper-cli

Local Faster Whisper command-line package for Lets Sub It.

`whisper-cli` transcribes media files with `faster-whisper` and writes validated WebVTT output for the backend subtitle pipeline.

## Development

Install dependencies from this directory:

```bash
uv sync --dev
```

Run the test suite:

```bash
uv run pytest
```
